import { css } from 'glamor';
import type { IGlassStyleProps } from './Glass.types';

// eslint-disable-next-line
export const Styles = ({}: IGlassStyleProps) => {
  return {
    root: css({}),
  };
};

export default Styles;
