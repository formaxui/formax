import { style } from '@formaxui/utils';
import type { IBaseStyleProps } from './Base.types';

// eslint-disable-next-line
export const Styles = ({}: IBaseStyleProps) => {
  return { root: style({}) };
};

export default Styles;
